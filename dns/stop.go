package dns

/*import (
	"fmt"
	"github.com/ben-han-cn/cement/shell"
)
*/
func StopDNS() {
	/*var command string = "ps -eaf|grep named|grep -v grep|awk '{print $2}'"
		var ret string
		var err error
		if ret, err = shell.Shell(command); err != nil {
		return fmt.Errorf("Stop Error!%w", err)
		}else if len(ret) > 0 {
		fmt.Println("is started! stop now!")
		var stop_cmd string = "kill -9 " + ret

	        if ret, err = cmd(stop_cmd);err != nil {
	        fmt.Printf("stop fail! return message:%s\n",ret)
	        fmt.Println(err)
	        }else {
	        fmt.Printf("stop success! return message:%s\n",ret)
	        }

		}else {
		fmt.Println("Nothing done!");
		}*/
}
